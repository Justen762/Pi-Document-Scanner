Requirements:
    - a venv
    - Python 3.13.3
    - tkinter (should be included by default)
    - requests (inside your venv run "pip3 install requests")

To Run (on mac at least):
    - "python3 GUI/PiScannerGUI.py"